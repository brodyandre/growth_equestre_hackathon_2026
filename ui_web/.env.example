# Porta onde a UI Web (Node.js) vai subir
PORT=3100

# URL do backend (API) existente no projeto Growth Equestre
# - Rodando local: http://localhost:3000
# - Rodando via Docker Compose (rede): http://backend:3000
BACKEND_URL=http://localhost:3000

# Endpoints (ajuste conforme as rotas reais do backend)
HEALTH_PATH=/health
SUMMARY_PATH=/partners/summary
KANBAN_PATH=/crm/board
KANBAN_MOVE_PATH=/crm/move

# NÃ­vel de log do proxy (debug | info | warn | error | silent)
PROXY_LOG_LEVEL=warn

CRM_NOTES_BASE_PATH=/crm/leads
CRM_MATCHES_BASE_PATH=/crm/leads

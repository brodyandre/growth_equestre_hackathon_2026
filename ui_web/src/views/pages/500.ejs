

<div class="card">
  <div class="card-title">Erro interno</div>
  <p class="muted mt-8">Ocorreu um erro no servidor da UI.</p>

  <pre class="code mt-12"><%= (error && error.stack) ? error.stack : error %></pre>

  <div class="btn-row mt-12">
    <a class="btn" href="/">Voltar ao Dashboard</a>
  </div>
</div>
